import { defineStore } from 'pinia';

const useProperties = defineStore('properties', {
  state: () => ({
    properties: [],
  }),
  getters: {
  },
  actions: {
  },
});

export default useProperties;
